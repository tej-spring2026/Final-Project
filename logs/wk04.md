# # Week 4 (2/10-2/18) - [Return Values]

## What I learned this week
[I learned about return values and was finally able to understand the clear difference between return and print(). Print just displays the value on the screen but doesn't actually produce an output from a funtion while return takes actually variable output out of function so that you can use it later. This week I also learned how to incrementally breakdown and develop a function to make the process easier. Lastly I learned about condiitonals like if/else statements and boolean/logical operators. ]

## Code/work I'm proud of (optional)
[def uses_none(word, forbidden):
    """Checks whether a word avoid forbidden letters.
    
    >>> uses_none('banana', 'xyz')
    True
    >>> uses_none('apple', 'efg')
    False
    >>> uses_none('tree', 'e')
    False
    """
    for letter in word.lower():
        if letter in forbidden.lower():
            return False
    return None


run_doctests(uses_none)
# the above code tests the doctest permeters to ensure the code is working correctly for its true/false logic. Although simple, I proud of this code b/c I was able to look through previous code frameworks to product this code without AI usuage. ]

## Challenges I faced
[I was finding it difficult to understand how to order the arguments and associated varibales/values when creating fucntions. However, I was able to overcome this by looking at previous, working functions with similar desired outputs to get the structure of these functions correct.]

## AI usage (if any)
[I tried using AI for some of Chapter 7 Jupyter Excersises but I didn't find it very helpful and instead just used pervous code examples to help generate my code.]

## Questions for next time
[I'm still finding creating/defining functions a bit diffcult; specfically structure and order of code for creating these functions.]